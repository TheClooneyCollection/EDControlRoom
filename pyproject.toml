[project]
name = "EDControlRoom"
version = "1.18.0"
description = "Elite Dangerous control-room and routine tooling"
requires-python = ">=3.12"
dependencies = [
  "httpx>=0.28",
  "playwright>=1.53",
  "PyYAML>=6.0",
  "pyobjc-framework-Quartz>=12.1; sys_platform == 'darwin'",
  "rich>=14.0",
  "starlette>=0.46",
  "textual>=0.60.0",
  "uvicorn>=0.34",
  "websockets>=15.0",
]

[project.optional-dependencies]
# Legacy / parked deps intentionally not included in any install extra:
# "requests~=2.21.0",
# "colorlog~=4.0.2",
# "pystray~=0.14.4",
# "PyAutoGUI~=0.9.42",
# "kthread~=0.2.2",
# "keyboard~=0.13.3",
dev = [
  "numpy>=1.26",
  "opencv-python>=4.8",
  "pytesseract>=0.3.10",
  "Pillow>=9.0; sys_platform == 'darwin'",
]

[tool.uv]
package = false
